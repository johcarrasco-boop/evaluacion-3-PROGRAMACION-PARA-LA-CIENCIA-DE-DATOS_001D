FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV ANALYTICS_DB_PATH=/app/data/processed/netflix_analytics.db
CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
